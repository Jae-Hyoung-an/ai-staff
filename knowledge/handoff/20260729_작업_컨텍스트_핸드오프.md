# 작업 컨텍스트 핸드오프 (2026-07-29 기준)

> 다른 컴퓨터/새 세션에서 작업을 이어가기 위한 스레드별 컨텍스트 요약.
> **새 세션 시작 시**: 이 문서 → 해당 스레드의 "핵심 파일" → `rules/data_query.md` 순으로 읽으면 컨텍스트 복원 완료.

---

## 스레드 A. 성북·성동 B존 전환 (7/20~, 최신·주력 작업)

**목표**: 성북·성동 지역을 일반 지역 → B존(벤더존)으로 전환. 동대문 이관 경험을 일반화한 SOP 기반 실행.

**상태**: 3-1 이관대상 상점 추출 완료 → 3-3 상점별 적용 할증 이관 **실행 중** (7/21 라이브 검증 리포트까지 생성).

**핵심 파일**: `projects/260720_성북성동_b존_전환/`
- `3-3_변경비교_리포트_20260721.md` — 이관 진행 현황 (스냅샷 7/20 886건 → 라이브 666건: 매핑 해제 434 / 자연만료 4 / 신규 218 / 금액변경 11 / 유지 437)
- `3-3_변경비교_상세_20260721.csv` — 건별 상세
- `3-3_상점별_적용할증_목록*.csv`, `3-3_상점별_적용할증_추출_요약.md` — 추출 원본/요약
- 도구: `tools_build_live_fee_csv.py`(라이브 Prime DB 할증 CSV 생성), `tools_diff_extrafee.py`(스냅샷 대비 diff)

**확정된 작업 방식**:
- 스냅샷(7/20) 기준 상점별 할증 목록 추출 → 신규 할증(#1517xx 대역)으로 재매핑 → 라이브 DB 재조회 후 diff 검증 사이클
- 신규 매핑분은 대부분 판가만 설정(원가=0), 금액 변경 11건도 원가 500→0 패턴 (원가는 벤더 체계로 분리되는 방향과 일치)

**미결 (다음 할 일)**:
1. 유지(미변경) 437건 검토 — 기상할증·요기요/법인 할증 등 이관 제외 대상과 잔여 이관 대상 구분 확정
2. 이관 완료 후 재검증 diff 1회 더 (최신 라이브 기준)
3. 관련 SOP(`일반 지역 → B존 전환 실행 SOP`, 커밋 659dd62) 에 이번 실행 경험 반영

## 스레드 B. 존/권역 구조 개편 PRD (구 Zone 요금제 개선, 6/29~7/13)

**목표**: 존(영업기준/판가, 중첩 불가)과 권역(벤더 배송/원가, 중첩 허용) 디커플링 + 회귀 오더·권역 외 할증 구현. **타임라인 10월 말~11월 초·중순** (송파·강남 진출 전 필수).

**상태**: 초기 PRD **v0.2 작성 완료** (개발팀장 리뷰 대기). 7/7 미팅에서 풀스펙 진행 확정, 메인 개발 PM 한영수님.

**핵심 파일**:
- `knowledge/2_planning/prd/zone_권역구조_개편/01_PRD.md` (v0.2) + [노션](https://app.notion.com/p/39ccf5759dc081f2be9dc3bbbbfd50e9)
- `projects/260629_zone_요금제_개선_회의자료/` (개괄 자료 + 7/7 Recap)
- 현행 정책 KB: `knowledge/domain/pricing/20260617_zone_요금제_정책.md`
- **AS-IS 화면 명세 ① (7/29 신규)**: `knowledge/1_discovery/product/zone_권역정책_관리_asis/`
  (00 개요·인덱스 / 01 화면별 상세 명세 / 02 기획 대비 차이·개선포인트 / screen_spec.yaml)
  + 스크린샷 8장 `resources/screenshots/260729_zone_권역정책_관리/`
- **AS-IS 화면 명세 ② (7/29 신규, 7개 메뉴)**: `knowledge/1_discovery/product/g4_zone_세트_관리_asis/`
  (00 개요·인덱스 / 01 G4 요금상품 4개 메뉴 / 02 ZONE·벤더·세트분배 3개 메뉴 / 03 개선포인트·확인필요 / screen_spec.yaml)
  + 스크린샷 35장 `resources/screenshots/260729_g4존세트_관리/`
  → ①+② 합쳐서 벤더존 운영 관리자 화면 **8개 메뉴 전체** 커버 (폴리곤 편집기만 제외)

**확정된 핵심 결정** (상세는 PRD §1.1):
- 요금제 서버에서 원가 제거 → 원가는 권역(벤더/라스트마일) 측 보유. 원가 확정 시점 = 오더 생성 → **배차 시점**
- 오더 3분류(권역 내/외/회귀, 출·도착지 기준) + AI 배차 우선순위(회귀·권역 내 우선, 저원가 우선)
- 원가 설정 = `ZONE 권역정책 관리` 개편 (벤더 배송 권역 편집기 + 원가 요금제 탭[요일/시간대별] + 원가할증 탭[권역 외 할증 신설])
- 세일즈 권역 = 지점의 픽업권역 / 원가 표 슬롯 = 단위물량 5슬롯 + 시간 외 1행 / honeymoon(우선배차) = 신규 상점 초기 오더 벤더 기사 우선

**7/29 추가 진행 — 현행 `ZONE 권역정책 관리` 화면 AS-IS 조사 완료**:
- 브라우저로 운영 인트라 직접 탐색(조회 전용, 저장/생성 미실행). 목록·생성·상세(기본정보/변경이력)·ZONE검색 모달·버전비교 모달 전체 기록
- **버전 관리가 이미 구현되어 운영 중**임을 확인 (`예약 저장` → 적용 시작일 미래 지정, v1/v2 버전 누적, 버전 diff 모달)
- 목적: 개편 목업 제작 + 개선 기획의 AS-IS 기준선 확보
- **발견한 문서-구현 불일치 3건**: ① 용어집 슬롯 시간대가 실제 설정과 다름(정책별 자유 입력임) ② 용어집의 "세트 물량 구성 탭"은 실제로는 섹션 ③ 벤더관리 PRD에서 스펙아웃된 "권역설정관리"가 이미 운영 중인데 분리 PRD가 없음
- **개선포인트 8건** 도출 (높음 4건: 용어 불일치 / `성공 슬롯 수` 상한 안내 없음 / 연결된 ZONE 미필터링 / 활성·비활성 전환 경로 없음)

**7/29 추가 진행 ② — G4 요금 상품 4개 + ZONE 관리 + 벤더 관리 + 세트분배 관리 AS-IS 조사 완료**:
- 조사 대상: `/zone/sales-pricings` `/zone/cost-pricings` `/zone/extra-fees` `/zone/promotions` `/sales/zones` `/vendor/vendors` `/vendor/set-allocations` (폴리곤 편집기 제외)
- 목록·상세·생성 화면 전체 + 모달 5개(ZONE 검색·폴리곤 검색·상점 검색·버전 비교·지점 드롭다운) + 탭 구성 기록. 조회 전용(저장·삭제 미실행)
- **운영 리스크 2건 발견**:
  ① **성북·성동이 라이브(7/21~22)했는데 G4 판가·원가 요금제가 연결돼 있지 않음** — 기존 `배송비 관리 > 요금제 관리`를 쓰는 중일 가능성이 높으나 미확인 (03번 문서 Q-1)
  ② **운영 중인 유일한 할증(`동대문 1_주말전체`)이 현재 검증 규칙을 통과하지 못함** — 심야 구간을 `24:00~27:00`이 아니라 `00:00~03:00`으로 입력. 실제 할증 미적용 사고일 수 있음 (B-2 / Q-11)
- **가장 중요한 미확인 항목**: **벤더 ↔ ZONE 연결 경로가 화면 어디에도 없음**. 벤더 상세의 `소속 Zone`은 읽기전용, 등록 화면에는 섹션 자체가 없는데 변경 이력에는 `존 연결` 기록이 남음. 시각 대조 결과 **세트분배 저장이 곧 존 연결**로 추정되나 확정 필요 (Q-7)
- **표시 오류 다수**: 판가·원가의 `적용 ZONE`이 실제 매핑이 있는데도 항상 0개로 표시(B-1) / 할증·프로모션 안내문구가 "원가 요금제"·"판가 요금제" 복붙 상태(B-3) / 벤더 이력의 `존 연결`에 ZONE명 대신 UUID 노출(B-10) / 벤더 상태를 `운영중`·`활성`·`ACTIVE` 3가지로 표기(B-11)
- **이슈 총 31건**: 버그 11 + 개선 8 + 확인필요 질문 12 (`03_개선포인트_및_확인필요사항.md`에 우선순위 Top 5와 다음 액션 6개까지 정리)
- **참고: 실운영 데이터가 매우 적음** — ZONE 6건 중 운영 3건, 벤더 26건 중 운영 8건(18건은 QA·해지 잔여물), 프로모션·지역폴리곤 할증은 실운영 0건

**미결 (다음 할 일)**:
1. PRD 리뷰 미팅 (개발팀장 초대) → 피드백 반영 v0.3
2. PRD §7 TBD 8건 결정 — 특히 TBD-1(기존 zone 요금제 원가 존치 여부)이 요금제 서버 개발 범위 좌우
3. PRD 확정 후 `02_기능명세.md` (케이스별 시나리오·엣지) 분리 작성
4. **AS-IS 조사 ① 후속** — `zone_권역정책_관리_asis/02_기획_대비_차이_및_개선포인트.md`의 "미해결 질문" 9건을 개발팀/라이더팀에 확인
   (특히 `성공 슬롯 수` 정의, 성북의 35가 의도값인지, ZONE 중복 연결 시 동작)
5. **AS-IS 조사 ② 후속 (우선순위 높음)** — `g4_zone_세트_관리_asis/03_개선포인트_및_확인필요사항.md`
   - Q-1(요금제 누락)·Q-7(벤더-ZONE 연결) 개발팀 확인 → **이 둘이 풀려야 ZONE 오픈 SOP 작성 가능**
   - Q-11 답변에 따라 B-2를 단순 UI 검증 문제 vs 실제 할증 미적용 사고로 판정 → 사고면 즉시 에스컬레이션
   - 표시 오류 6건(B-1·B-3·B-6·B-7·B-9·B-10) 일괄 수정 티켓
   - 운영 벤더 8건 계약 종료일 현황표 작성 (백호는 **2026-08-21 만료**, 알림·목록 컬럼 없어 놓치기 쉬움)
   - QA 잔여 데이터 정리 협의 (벤더 26건 중 18건)
6. **QA 환경에서 미확인 동작 검증** — 저장/예약저장 플로우, 적용 시작일 입력 UI, 검증 에러 문구,
   벤더 상태 전이(대기→운영중 버튼이 화면에 없음), 상점 검색 모달의 `코드로 추가`/`엑셀로 추가` 탭
7. 용어집(`벤더관리시스템/01_용어집.md`) 슬롯 시간대·"탭" 표기 수정 여부 결정

## 스레드 1. 벤더 운영 대시보드 (7/2~7/9)

**목표**: Metabase(Snowflake db_id=12) 벤더 운영 대시보드. 수요→배송퀄리티→공급→수익성 4섹션.

**상태**: SQL 13종 + 조립 가이드 **완성**, Metabase 카드/대시보드 조립도 **완료**(사용자 직접). 운영 검증 단계.

**핵심 파일**: `projects/260702_벤더_운영_대시보드/조립_가이드.md` (지표 정의·변경이력·배치 가이드 전부 포함) + `sql/` 13개

**확정된 핵심 결정** (상세는 조립_가이드 §3~4):
- **시간 기준 = `CREATED_AT`** (상점 주문 시점). `SUBMITTED_AT`은 배정 실패 시 전체공개 전환 시점 → 사용 금지 (`rules/data_query.md`에 KB화)
- **스코핑 2종**: 수요·퀄리티·수익성 = 권역 접수(`MONITORING_PARTNER_ID`→`ZONES.EXTERNAL_PARTNER_ID`) / 공급 = 벤더 기준(`RAW_VENDORSET`)
- **물량타입**: G4(`SALES_TYPE='직영센터'`, 최우선) → OD(요기요OD) → 법인(메쉬프라임) → 로컬 → 기타. 시리즈는 법인/OD/G4+전체 4개로 통일
- **SLA** = 생성→완료 40분 (`DATEDIFF <= 2400`). `COMPLETE_TIME` 컬럼은 SUBMITTED 기준이라 미사용 (생성 기준 90.9% vs 접수 기준 95.6%)
- **거절률(C3)** = PRD SD2-5/6: 거절건(적극거절20+만료30+수락후취소60) ÷ 완료오더
- **수익성(D)**: 완료건만. 판가=`STORE_BILLABLE_FEE÷1.1`(VAT 제외), 원가=`AGENT_FEE+AGENT_EXTRA_FEE`(VAT 미포함 확정)
- B3 분모=완료건(미배차는 전부 취소 상태로 검증됨), B4는 기타(일반기사) 제외 2시리즈

**발견된 인사이트**:
- 프렌즈 수행건 CM **−2,303원/건** vs 벤더세트 +38원/건 (6/29주, AGENT_EXTRA_FEE 프로모션 영향) → 벤더 공급 확대의 경제적 근거
- 운영 존은 현재 `동대문 1`(EXTERNAL_PARTNER_ID=4563) 하나. 나머지 17개는 QA/테스트

**미결 (다음 할 일)**:
1. **벤더 실원가**: 주간 세트 정산 기준 확정되면 D2/D3의 벤더세트 원가 교체 (현재 기사원가 잠정)
2. **상점 264031** (`[직영A] 스시화 동대문점`): 마스터가 로컬/G3A로 남아있어 G4 분류 12건 누락 — 사용자가 마스터 정정 진행 중. 정정 후 이름기준(1,020) vs SALES_TYPE기준(1,008) 재검증
3. 거절률 세부(동시제안 round SD2-19, 미사용 슬롯 SD2-21)는 미러 플래그 부재로 근사 중
4. (아이디어) 접수전환율(SUBMITTED 도달 비율) = 배차 실패 조기경보 카드 — 제안만 된 상태

---

## 스레드 2. 기상악화 수급조정 SOP (7/6)

**산출물**: `projects/260706_기상악화_수급조정_sop/기상악화_수급조정_sop.md` (v0.3)
**요지**: 기상악화 시 강수량이 아닌 **기사/오더 비율**로 0~4단계 대응. 대상은 G4 상점 오더(협의 완료), OD·법인은 확대 협의 예정.
**미결**: 문서 §7 적용 대상·액션 플랜의 후속 협의.

## 스레드 4. 성북·성동 권역 상점추출 (6/30) — 완결

**산출물**: `projects/260630_성북성동_권역_상점추출/` (final_result.csv, 결과_리포트.md) + 도구 `tools/admin_dong_matcher/`
**요지**: 상점 위경도→행정동 폴리곤 매칭(point-in-polygon)으로 권역 내 상점 추출. 스레드 A(B존 전환)의 선행 작업. 도구는 `rules/tools.md`에 등록됨.

## 스레드 5. 오더 히트맵 / 라이브 대시보드 (6/17~6/29) — 완결

**산출물**: `tools/heatmap_builder/`(성북구 오더 히트맵, GitHub Pages 배포), `tools/lastmile_live_dashboard/`(Flask+Metabase API), `projects/260629_lastmile_운영_대시보드/`
**요지**: 완결된 작업. 히트맵은 필터 4종·성동구 병합까지 반영됨.

## 스레드 6. 벤더관리 PRD 정리 (6/24~6/30)

**산출물**: `knowledge/2_planning/prd/벤더관리시스템/` 00~07 (특히 `07_변경사항_정리.md` = SD2-* 티켓 후처리)
**요지**: 거절률 산정 정책(SD2-5/6), 주간 수급계획 미팅 구성, 회의 템플릿. 대시보드 스레드의 지표 정의 원천.
**미결**: PRD §미정 항목 — 거절률 분모 범위, 배정완료시간 정의, 미사용 슬롯 제외 범위, 마스터 로그인 권한. (존/권역 개편 확정 시 §2.1 존 1:1 매핑 전제 개정 필요 — 스레드 B 참조)

---

## 공통 인프라 (모든 스레드가 참조)

| 파일 | 내용 |
|------|------|
| `rules/data_query.md` | Snowflake 조회 규칙 + **ORDERS 시간 컬럼 의미(CREATED/SUBMITTED)** + 벤더세트 테이블·상태코드 |
| `rules/tools.md` | 도구 레지스트리 (admin_dong_matcher, heatmap_builder 등) |
| `AGENTS.md` | AI 협업 규칙 (So What 원칙, projects/ 네이밍, §13 핸드오프 규칙 등) |

**데이터 접근**: Metabase MCP `execute_query`(db_id=12 Snowflake). 한글 별칭은 `"따옴표"` 필수, 2,000행 제한.

**인트라넷 화면 직접 확인** (7/29 확보): Cursor 내장 브라우저(`Settings > Tools & MCP > Browser Automation` → `Browser Tab`)로 AI가 `intra-lite.vroong.com`을 직접 조회·스크린샷 가능. 세션(쿠키)은 워크스페이스 단위로 유지되므로 한 번 로그인하면 이후 세션에서도 재사용됨. **주의 2가지**: ① 같은 계정을 외부 크롬에서 동시 로그인하면 "계정 사용 불가"로 세션이 끊김 → 크롬 쪽 인트라 탭은 닫아둘 것 ② 운영 환경이므로 저장·생성·삭제 버튼은 클릭 금지 (조회만).
**노션 주요 페이지**: [존/권역 PRD v0.2](https://app.notion.com/p/39ccf5759dc081f2be9dc3bbbbfd50e9) · [7/7 Recap & Todo](https://app.notion.com/p/396cf5759dc081ab8097f7468d7717b9) · [개선 방향 개괄](https://app.notion.com/p/38ecf5759dc08160a8cdeaddb8f66a0c)
